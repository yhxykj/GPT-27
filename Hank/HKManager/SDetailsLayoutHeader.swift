
import Foundation
import UIKit
import SwiftKeychainWrapper

let AppUrl = "https://chat.smyhxy.top/api"
let WebUrl = "wss://chat.smyhxy.top/websocket/"

let PrivacyUrl = "https://cb3i458lt6.feishu.cn/docx/EfsjdNibdoIOQNx1qfbcrEZWnrg"
let UserAgreent = "https://cb3i458lt6.feishu.cn/docx/RLOedPW40opAH0xhBwHc68a7nSg"
let MonthUrl = "https://cb3i458lt6.feishu.cn/docx/OFdjdoL5yoJdptxKr7icZRAjnwh"

let APPID = "6743840748"
let AppType = "27"
var modelType: String = "1"
var pointNumber: Int = 0
var promptP: Int = 0
var promptN: Int = 0
var isKout: Bool = false
var isGuidance: Int = 1

let keyWindow = UIApplication.shared.keyWindow
let Screen_height = UIScreen.main.bounds.size.height
let Screen_width = UIScreen.main.bounds.size.width

var collectChats: [String] = NSMutableArray() as! [String]

struct DARSocketRegister {
var home_size: Double = 0.0
var numberlabel_min: Float = 0.0



    static let serviceName = "userKey_"
}

func getAccountNumberIdentifier() -> String? {
       var keywords8: String! = String(cString: [107,109,101,97,110,115,0], encoding: .utf8)!
    var deepL: String! = String(cString: [99,111,110,116,114,111,108,115,0], encoding: .utf8)!
    _ = deepL
    var userdataI: Double = 1.0
    var amountN: String! = String(cString: [122,95,50,48,0], encoding: .utf8)!
   repeat {
       var respond2: String! = String(cString: [115,101,110,100,97,108,108,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &respond2) { pointer in
    
      }
       var leanP: [Any]! = [48, 896, 955]
      withUnsafeMutablePointer(to: &leanP) { pointer in
    
      }
       var int_jE: String! = String(cString: [102,111,108,100,101,114,0], encoding: .utf8)!
       var outud: Bool = false
       var dict2: [Any]! = [String(cString: [109,111,100,105,102,105,101,100,95,53,95,55,51,0], encoding: .utf8)!, String(cString: [110,101,97,114,101,110,100,0], encoding: .utf8)!]
       var iconL: [Any]! = [238, 169]
         int_jE.append("\(1)")
         dict2.append(1)
      if !int_jE.hasPrefix("\(outud)") {
          var atts_: String! = String(cString: [111,110,121,120,99,0], encoding: .utf8)!
          var contentT: String! = String(cString: [114,100,98,120,0], encoding: .utf8)!
          var resultR: String! = String(cString: [109,117,108,116,105,102,114,97,109,101,95,118,95,53,52,0], encoding: .utf8)!
          var controllers3: Int = 5
          _ = controllers3
          var gundp: Float = 2.0
         outud = 78 < resultR.count
         atts_.append("\(controllers3 | 1)")
         contentT.append("\((atts_.count - (outud ? 1 : 1)))")
         controllers3 += iconL.count - respond2.count
         gundp -= Float(dict2.count)
      }
         respond2 = "\((1 ^ (outud ? 5 : 2)))"
      repeat {
         int_jE = "\(int_jE.count / (Swift.max(10, dict2.count)))"
         if int_jE == (String(cString:[104,100,97,119,99,110,105,122,118,48,0], encoding: .utf8)!) {
            break
         }
      } while (int_jE == (String(cString:[104,100,97,119,99,110,105,122,118,48,0], encoding: .utf8)!)) && (int_jE.count <= 2)
      while ((dict2.count / (Swift.max(int_jE.count, 5))) > 4 || 4 > (dict2.count / (Swift.max(4, int_jE.count)))) {
         dict2 = [(respond2 == (String(cString:[98,0], encoding: .utf8)!) ? respond2.count : int_jE.count)]
         break
      }
          var speedsh: String! = String(cString: [99,111,100,101,99,105,100,0], encoding: .utf8)!
          var seti: Bool = true
          var myloadingw: Bool = false
         withUnsafeMutablePointer(to: &myloadingw) { pointer in
    
         }
         int_jE = "\((respond2.count - (outud ? 4 : 1)))"
         speedsh = "\(((outud ? 3 : 5)))"
         seti = !outud
         myloadingw = 96 >= speedsh.count || leanP.count >= 96
      repeat {
         dict2 = [3]
         if 198403 == dict2.count {
            break
         }
      } while (4 == dict2.count) && (198403 == dict2.count)
         respond2.append("\(respond2.count)")
      repeat {
          var attsH: [Any]! = [UILabel()]
          var shou1: Bool = false
         withUnsafeMutablePointer(to: &shou1) { pointer in
                _ = pointer.pointee
         }
          var minimum5: Double = 1.0
          _ = minimum5
         respond2 = "\((1 >> (Swift.min(labs(Int(minimum5 > 237103744.0 || minimum5 < -237103744.0 ? 74.0 : minimum5)), 2))))"
         attsH.append(dict2.count)
         shou1 = attsH.count >= 83 && minimum5 >= 95.97
         if 3070634 == respond2.count {
            break
         }
      } while ((4 >> (Swift.min(5, dict2.count))) < 5 && (respond2.count >> (Swift.min(labs(4), 5))) < 3) && (3070634 == respond2.count)
      while (iconL.count >= int_jE.count) {
         int_jE = "\(((outud ? 3 : 1) + respond2.count))"
         break
      }
      keywords8 = "\(((outud ? 3 : 1) ^ 2))"
      if keywords8 == (String(cString:[108,119,98,110,104,117,120,0], encoding: .utf8)!) {
         break
      }
   } while (keywords8 == (String(cString:[108,119,98,110,104,117,120,0], encoding: .utf8)!)) && ((3.16 - userdataI) < 5.89)

   if deepL.count <= (Int(userdataI > 324264276.0 || userdataI < -324264276.0 ? 84.0 : userdataI)) {
       var datasL: Int = 5
       var navgationZ: String! = String(cString: [121,99,111,99,103,114,103,98,97,0], encoding: .utf8)!
       var n_centerW: String! = String(cString: [116,111,111,98,105,103,0], encoding: .utf8)!
       _ = n_centerW
          var statuslabelm: String! = String(cString: [100,111,99,117,109,101,110,116,115,0], encoding: .utf8)!
         datasL <<= Swift.min(5, labs(3))
         statuslabelm = "\(navgationZ.count)"
      while (n_centerW == navgationZ) {
          var emptyV: String! = String(cString: [109,117,110,108,111,99,107,0], encoding: .utf8)!
          _ = emptyV
          var sendM: Double = 5.0
          var inputJ: String! = String(cString: [100,101,116,101,114,109,105,110,101,95,51,95,54,55,0], encoding: .utf8)!
          var pathsc: [Any]! = [UILabel()]
          var promptc: Float = 5.0
         navgationZ = "\(navgationZ.count >> (Swift.min(labs(3), 1)))"
         emptyV = "\((datasL & Int(sendM > 276952978.0 || sendM < -276952978.0 ? 31.0 : sendM)))"
         sendM /= Swift.max(1, Double(1))
         inputJ = "\(navgationZ.count + 3)"
         pathsc.append(emptyV.count % (Swift.max(1, 5)))
         promptc -= Float(inputJ.count)
         break
      }
      while (2 > (4 << (Swift.min(3, navgationZ.count))) && (navgationZ.count << (Swift.min(labs(4), 5))) > 3) {
          var linesu: Double = 5.0
         withUnsafeMutablePointer(to: &linesu) { pointer in
                _ = pointer.pointee
         }
         navgationZ.append("\(1)")
         linesu /= Swift.max(5, Double(1 | navgationZ.count))
         break
      }
      repeat {
          var labelN: Double = 0.0
         n_centerW = "\(3)"
         labelN += Double(navgationZ.count | 3)
         if n_centerW.count == 467291 {
            break
         }
      } while (n_centerW.count == 467291) && (n_centerW == String(cString:[56,0], encoding: .utf8)! && navgationZ != String(cString:[110,0], encoding: .utf8)!)
          var m_view5: Double = 0.0
          var socketx: String! = String(cString: [109,117,116,101,120,0], encoding: .utf8)!
          var controllersL: String! = String(cString: [99,111,110,116,105,110,117,101,0], encoding: .utf8)!
          _ = controllersL
         datasL ^= 2 | n_centerW.count
         m_view5 -= Double(navgationZ.count)
         socketx = "\((Int(m_view5 > 181287864.0 || m_view5 < -181287864.0 ? 87.0 : m_view5) % 1))"
         controllersL = "\(controllersL.count + 1)"
      while ((n_centerW.count ^ 1) > 5 && (1 ^ datasL) > 2) {
          var userm: Float = 5.0
          var contextF: Double = 1.0
          _ = contextF
          var parametersb: String! = String(cString: [114,97,110,100,0], encoding: .utf8)!
          var subring7: String! = String(cString: [118,97,100,107,104,122,0], encoding: .utf8)!
          var speakw: String! = String(cString: [105,100,101,110,116,105,102,121,0], encoding: .utf8)!
         n_centerW.append("\(1)")
         userm += Float(1 * datasL)
         contextF -= (Double(3 | Int(userm > 176720879.0 || userm < -176720879.0 ? 47.0 : userm)))
         parametersb.append("\(subring7.count ^ 3)")
         subring7 = "\(1)"
         speakw.append("\(parametersb.count)")
         break
      }
         n_centerW.append("\(3)")
       var recognizerr: Double = 5.0
         n_centerW = "\(1)"
         recognizerr *= (Double((String(cString:[56,0], encoding: .utf8)!) == navgationZ ? navgationZ.count : datasL))
      deepL = "\(2)"
   }

   if deepL.count > (Int(userdataI > 80388647.0 || userdataI < -80388647.0 ? 17.0 : userdataI)) {
      deepL = "\((deepL.count >> (Swift.min(2, labs(Int(userdataI > 363756330.0 || userdataI < -363756330.0 ? 23.0 : userdataI))))))"
   }
    if let account_number = KeychainWrapper.standard.string(forKey: DARSocketRegister.serviceName) {
        return account_number
    }
    
    guard let UUID = UIDevice.current.identifierForVendor?.uuidString else {
        return nil
    }
    
    do {
        KeychainWrapper.standard.set(UUID, forKey: DARSocketRegister.serviceName)
   while (!deepL.contains("\(keywords8.count)")) {
       var channelw: Double = 0.0
       var urlsM: String! = String(cString: [101,99,111,117,110,116,95,106,95,50,55,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &urlsM) { pointer in
    
      }
       var insetz: Bool = false
      withUnsafeMutablePointer(to: &insetz) { pointer in
    
      }
       var statex: String! = String(cString: [112,111,105,110,116,101,114,0], encoding: .utf8)!
       _ = statex
       var coverU: Float = 2.0
      for _ in 0 ..< 1 {
         insetz = coverU < 36.69
      }
       var codinggy: Float = 3.0
      if (coverU + codinggy) > 1.80 {
          var origina: Int = 4
          var iconT: Double = 3.0
          _ = iconT
         codinggy -= (Float((insetz ? 3 : 4)))
         origina *= 2
         iconT /= Swift.max((Double(urlsM.count + (insetz ? 1 : 4))), 5)
      }
      for _ in 0 ..< 1 {
         statex = "\((Int(coverU > 314012023.0 || coverU < -314012023.0 ? 63.0 : coverU) ^ urlsM.count))"
      }
         urlsM = "\((Int(coverU > 330340992.0 || coverU < -330340992.0 ? 48.0 : coverU) & 2))"
         insetz = 15.42 > channelw && 15.42 > codinggy
       var systemo: [String: Any]! = [String(cString: [97,116,99,104,101,114,0], encoding: .utf8)!:281, String(cString: [112,114,101,116,119,105,100,100,108,101,0], encoding: .utf8)!:632]
       var max_2u: [String: Any]! = [String(cString: [109,111,100,101,109,118,99,111,115,116,0], encoding: .utf8)!:385, String(cString: [100,116,111,114,0], encoding: .utf8)!:731, String(cString: [117,116,105,109,101,0], encoding: .utf8)!:980]
      withUnsafeMutablePointer(to: &max_2u) { pointer in
             _ = pointer.pointee
      }
      if 1.52 == coverU || (1.52 - coverU) == 2.29 {
          var right9: String! = String(cString: [100,105,114,101,99,116,120,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &right9) { pointer in
                _ = pointer.pointee
         }
          var deletebuttonm: String! = String(cString: [97,117,100,105,111,100,115,112,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &deletebuttonm) { pointer in
    
         }
          var primeA: [String: Any]! = [String(cString: [108,95,57,54,95,97,103,101,110,116,115,0], encoding: .utf8)!:708, String(cString: [103,114,97,121,114,103,98,0], encoding: .utf8)!:753]
         withUnsafeMutablePointer(to: &primeA) { pointer in
    
         }
          var tableeU: Double = 1.0
          _ = tableeU
          var yhlogox: String! = String(cString: [122,95,51,55,95,112,105,120,102,109,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &yhlogox) { pointer in
                _ = pointer.pointee
         }
         coverU /= Swift.max(Float(max_2u.count), 3)
         right9.append("\((2 << (Swift.min(labs(Int(channelw > 105090228.0 || channelw < -105090228.0 ? 81.0 : channelw)), 4))))")
         deletebuttonm = "\(systemo.values.count)"
         primeA["\(codinggy)"] = (Int(codinggy > 261855787.0 || codinggy < -261855787.0 ? 70.0 : codinggy))
         tableeU /= Swift.max(Double(max_2u.values.count), 2)
         yhlogox = "\(((String(cString:[118,0], encoding: .utf8)!) == statex ? Int(coverU > 275271802.0 || coverU < -275271802.0 ? 73.0 : coverU) : statex.count))"
      }
      while (Double(systemo.values.count) == channelw) {
         systemo = ["\(max_2u.count)": max_2u.count - 2]
         break
      }
          var requesta: [Any]! = [547, 60, 558]
          var rowsA: [String: Any]! = [String(cString: [116,114,105,109,109,101,100,0], encoding: .utf8)!:UILabel()]
          var visiblen: String! = String(cString: [99,97,108,105,110,103,95,112,95,57,56,0], encoding: .utf8)!
         statex.append("\(urlsM.count * 2)")
         requesta = [(urlsM == (String(cString:[75,0], encoding: .utf8)!) ? urlsM.count : Int(codinggy > 158511498.0 || codinggy < -158511498.0 ? 81.0 : codinggy))]
         rowsA = ["\(systemo.count)": systemo.keys.count / (Swift.max(visiblen.count, 8))]
         visiblen = "\(systemo.keys.count ^ visiblen.count)"
         systemo = [statex: ((insetz ? 1 : 3) % (Swift.max(1, 2)))]
          var configurationh: String! = String(cString: [119,101,101,107,100,97,121,0], encoding: .utf8)!
          _ = configurationh
          var drawv: String! = String(cString: [109,97,110,97,103,101,114,0], encoding: .utf8)!
          var resizedH: String! = String(cString: [102,101,116,99,104,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &resizedH) { pointer in
                _ = pointer.pointee
         }
         insetz = drawv.count >= urlsM.count
         configurationh.append("\((Int(codinggy > 370284011.0 || codinggy < -370284011.0 ? 55.0 : codinggy) | 2))")
         resizedH = "\(resizedH.count)"
         systemo = ["\(insetz)": urlsM.count >> (Swift.min(labs(2), 2))]
       var l_animation1: Double = 0.0
       _ = l_animation1
          var textq: Double = 2.0
         systemo = [urlsM: ((insetz ? 4 : 1))]
         textq += (Double(Int(l_animation1 > 236026850.0 || l_animation1 < -236026850.0 ? 82.0 : l_animation1) & max_2u.keys.count))
      keywords8.append("\(keywords8.count)")
      break
   }
        return UUID
    }
    
       var finishh: String! = String(cString: [118,105,100,101,111,99,111,100,101,99,0], encoding: .utf8)!
       _ = finishh
      repeat {
         finishh = "\(2)"
         if finishh == (String(cString:[122,57,121,57,54,48,98,106,0], encoding: .utf8)!) {
            break
         }
      } while (!finishh.hasSuffix(finishh)) && (finishh == (String(cString:[122,57,121,57,54,48,98,106,0], encoding: .utf8)!))
          var closep: Int = 5
         finishh.append("\(closep)")
      for _ in 0 ..< 1 {
          var completionl: String! = String(cString: [100,121,110,108,111,99,107,0], encoding: .utf8)!
          _ = completionl
          var mine6: String! = String(cString: [115,116,114,105,112,101,0], encoding: .utf8)!
         finishh.append("\(mine6.count + 3)")
         completionl.append("\(completionl.count & 2)")
      }
      keywords8.append("\(deepL.count)")
      amountN.append("\(2 << (Swift.min(2, deepL.count)))")
      amountN = "\(amountN.count ^ 3)"
}

func convertImageToBase64(image: UIImage) -> String? {
       var dicW: Bool = false
    var picturen: Bool = false
   withUnsafeMutablePointer(to: &picturen) { pointer in
    
   }
   while (dicW) {
      picturen = !picturen && dicW
      break
   }

       var paint6: [String: Any]! = [String(cString: [112,97,110,100,105,110,103,0], encoding: .utf8)!:String(cString: [109,105,108,105,111,110,115,0], encoding: .utf8)!, String(cString: [115,121,110,99,112,111,105,110,116,0], encoding: .utf8)!:String(cString: [115,118,113,101,110,99,0], encoding: .utf8)!]
       _ = paint6
       var text1: Double = 3.0
      withUnsafeMutablePointer(to: &text1) { pointer in
             _ = pointer.pointee
      }
       var read7: Double = 5.0
       _ = read7
         paint6["\(read7)"] = 2
      if (read7 / (Swift.max(text1, 3))) > 1.98 || (1.98 / (Swift.max(2, read7))) > 1.30 {
         text1 /= Swift.max(Double(paint6.values.count), 1)
      }
      dicW = nil != paint6["\(picturen)"]
    if let imageData = image.pngData() {
        return imageData.base64EncodedString(options: [])
    }
    return nil
   while (!picturen) {
      picturen = (!picturen ? dicW : !picturen)
      break
   }
       var eaderZ: String! = String(cString: [115,111,117,110,0], encoding: .utf8)!
       _ = eaderZ
       var editf: Bool = false
      repeat {
         editf = !eaderZ.contains("\(editf)")
         if editf ? !editf : editf {
            break
         }
      } while (!editf || eaderZ.count >= 2) && (editf ? !editf : editf)
         eaderZ.append("\(((editf ? 2 : 5)))")
      while (eaderZ.hasSuffix("\(editf)")) {
         eaderZ = "\(((editf ? 1 : 3) | 3))"
         break
      }
         editf = editf || eaderZ.count >= 92
      repeat {
         eaderZ.append("\(3)")
         if (String(cString:[102,110,55,106,114,109,103,0], encoding: .utf8)!) == eaderZ {
            break
         }
      } while ((String(cString:[102,110,55,106,114,109,103,0], encoding: .utf8)!) == eaderZ) && (eaderZ.count >= 5)
          var sectionC: String! = String(cString: [114,101,113,117,105,114,101,109,101,110,116,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &sectionC) { pointer in
                _ = pointer.pointee
         }
          var editR: Int = 4
         eaderZ = "\(eaderZ.count)"
         sectionC = "\(((editf ? 1 : 3)))"
         editR |= 1
      picturen = eaderZ.contains("\(editf)")
}

func takeScreenshot(_ view: UIView) -> UIImage? {
       var main_dr: String! = String(cString: [115,97,118,105,110,103,115,0], encoding: .utf8)!
    var browserM: [Any]! = [String(cString: [109,105,100,110,105,103,104,116,0], encoding: .utf8)!, String(cString: [101,120,112,114,108,105,115,116,95,50,95,57,49,0], encoding: .utf8)!, String(cString: [98,105,110,98,110,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &browserM) { pointer in
    
   }
    var materialq: Double = 3.0
      materialq += Double(browserM.count)
      main_dr = "\(3 << (Swift.min(1, main_dr.count)))"

      main_dr.append("\(((String(cString:[83,0], encoding: .utf8)!) == main_dr ? main_dr.count : Int(materialq > 188834867.0 || materialq < -188834867.0 ? 44.0 : materialq)))")
      browserM = [main_dr.count | 1]
    let stop = UIGraphicsImageRenderer(size: view.bounds.size)
    let itemImage = stop.image { (context) in
        view.layer.render(in: context.cgContext)
    }
    return itemImage
   repeat {
       var verity1: Bool = false
       var observationsf: Float = 5.0
       var giff: Bool = true
      withUnsafeMutablePointer(to: &giff) { pointer in
    
      }
      if 2.34 < observationsf {
          var requestr: String! = String(cString: [117,110,105,113,117,101,0], encoding: .utf8)!
          var lineo: Double = 1.0
          _ = lineo
         giff = lineo < 82.19
         requestr.append("\(((verity1 ? 5 : 1) - (giff ? 2 : 3)))")
      }
      if giff && (4.36 - observationsf) < 4.18 {
          var ustomw: Float = 2.0
         observationsf -= (Float(2 << (Swift.min(labs(Int(ustomw > 286168508.0 || ustomw < -286168508.0 ? 91.0 : ustomw)), 1))))
      }
      repeat {
          var collectr: Int = 0
          var connectq: String! = String(cString: [108,101,101,119,97,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &connectq) { pointer in
    
         }
          var backgroundt: Double = 0.0
          var randomJ: String! = String(cString: [100,108,115,121,109,0], encoding: .utf8)!
          _ = randomJ
         observationsf *= Float(3 / (Swift.max(collectr, 9)))
         connectq.append("\(((verity1 ? 5 : 5) >> (Swift.min(labs(Int(backgroundt > 276609570.0 || backgroundt < -276609570.0 ? 80.0 : backgroundt)), 4))))")
         backgroundt -= (Double((String(cString:[112,0], encoding: .utf8)!) == connectq ? Int(backgroundt > 112018328.0 || backgroundt < -112018328.0 ? 66.0 : backgroundt) : connectq.count))
         randomJ = "\(collectr >> (Swift.min(2, labs(3))))"
         if 2611877.0 == observationsf {
            break
         }
      } while (2611877.0 == observationsf) && (4.98 >= observationsf)
       var enterC: [String: Any]! = [String(cString: [119,122,97,101,115,0], encoding: .utf8)!:474, String(cString: [105,110,116,101,114,115,112,101,114,115,101,100,0], encoding: .utf8)!:351]
       _ = enterC
         giff = observationsf < 3.3
         giff = (!giff ? !verity1 : giff)
      repeat {
         verity1 = (15 < (enterC.count + (!giff ? enterC.count : 93)))
         if verity1 ? !verity1 : verity1 {
            break
         }
      } while (verity1 ? !verity1 : verity1) && ((5.71 + observationsf) >= 1.63)
      for _ in 0 ..< 3 {
         verity1 = enterC.count > 30 || giff
      }
      while (enterC.keys.count == 1) {
         giff = verity1
         break
      }
      materialq /= Swift.max(5, Double(1))
      if 4310780.0 == materialq {
         break
      }
   } while (4310780.0 == materialq) && (5 < (Int(materialq > 249906526.0 || materialq < -249906526.0 ? 52.0 : materialq) - main_dr.count) && 5 < (Int(materialq > 37668430.0 || materialq < -37668430.0 ? 76.0 : materialq) - main_dr.count))
       var serverh: String! = String(cString: [108,105,98,115,119,105,102,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &serverh) { pointer in
             _ = pointer.pointee
      }
       var symbolp: Float = 5.0
      for _ in 0 ..< 1 {
         serverh = "\(1)"
      }
      repeat {
          var endk: Bool = true
          var modity9: String! = String(cString: [101,120,116,114,97,99,102,103,0], encoding: .utf8)!
          var confirmbuttonz: String! = String(cString: [108,111,110,103,105,116,117,100,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &confirmbuttonz) { pointer in
    
         }
          var regionW: String! = String(cString: [100,111,108,98,121,0], encoding: .utf8)!
         symbolp += Float(confirmbuttonz.count ^ 1)
         endk = ((Int(symbolp > 138151059.0 || symbolp < -138151059.0 ? 5.0 : symbolp) + regionW.count) < 7)
         modity9 = "\((confirmbuttonz.count ^ Int(symbolp > 373406996.0 || symbolp < -373406996.0 ? 17.0 : symbolp)))"
         regionW.append("\(regionW.count >> (Swift.min(labs(3), 5)))")
         if symbolp == 1654926.0 {
            break
         }
      } while (symbolp == 1654926.0) && (symbolp >= 1.44)
      repeat {
          var templ: String! = String(cString: [118,101,114,115,105,111,110,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &templ) { pointer in
                _ = pointer.pointee
         }
          var rightbuttonA: String! = String(cString: [117,110,98,105,110,100,0], encoding: .utf8)!
          _ = rightbuttonA
          var networkd: Double = 2.0
          var navigationL: String! = String(cString: [115,116,111,112,112,101,100,0], encoding: .utf8)!
          var portraitb: String! = String(cString: [114,95,54,49,95,99,114,111,115,115,98,97,114,0], encoding: .utf8)!
         serverh = "\((Int(symbolp > 383438534.0 || symbolp < -383438534.0 ? 95.0 : symbolp)))"
         templ.append("\(rightbuttonA.count)")
         rightbuttonA.append("\(navigationL.count ^ rightbuttonA.count)")
         networkd -= (Double(Int(networkd > 101906544.0 || networkd < -101906544.0 ? 78.0 : networkd)))
         navigationL = "\(3)"
         portraitb.append("\(navigationL.count)")
         if serverh.count == 3265285 {
            break
         }
      } while (4.84 == symbolp) && (serverh.count == 3265285)
      repeat {
          var signf: Double = 4.0
          var repairU: [Any]! = [672.0]
          var sort_: [Any]! = [String(cString: [98,105,116,105,122,101,110,0], encoding: .utf8)!]
          var savedrawI: String! = String(cString: [97,116,116,114,97,99,116,0], encoding: .utf8)!
          var weixinlabelX: Bool = false
         serverh.append("\(serverh.count - 2)")
         signf *= (Double(Int(signf > 244295616.0 || signf < -244295616.0 ? 97.0 : signf) + 3))
         repairU.append(1)
         sort_.append(1)
         savedrawI = "\(repairU.count)"
         weixinlabelX = symbolp == 97.14
         if serverh.count == 3508453 {
            break
         }
      } while ((1.62 + symbolp) == 5.73 && 5 == (Int(symbolp > 71835834.0 || symbolp < -71835834.0 ? 54.0 : symbolp) + 2)) && (serverh.count == 3508453)
          var urlsm: String! = String(cString: [115,104,111,117,108,100,0], encoding: .utf8)!
          var liholderlabely: String! = String(cString: [115,99,113,117,101,114,121,0], encoding: .utf8)!
          var moreb: String! = String(cString: [110,117,109,98,101,114,95,48,95,49,57,0], encoding: .utf8)!
          _ = moreb
         serverh.append("\(serverh.count / (Swift.max(1, 8)))")
         urlsm = "\(moreb.count)"
         liholderlabely = "\((moreb == (String(cString:[113,0], encoding: .utf8)!) ? moreb.count : serverh.count))"
      repeat {
         symbolp /= Swift.max((Float(serverh == (String(cString:[53,0], encoding: .utf8)!) ? Int(symbolp > 28595800.0 || symbolp < -28595800.0 ? 85.0 : symbolp) : serverh.count)), 3)
         if 4069667.0 == symbolp {
            break
         }
      } while (4069667.0 == symbolp) && (2 >= (serverh.count / 3) && 2.93 >= (symbolp / 1.63))
      browserM.append(3 << (Swift.min(3, main_dr.count)))
}

func teamAppNavigation(_ view: UIView) -> UIViewController? {
       var disconnectx: String! = String(cString: [99,117,118,105,100,0], encoding: .utf8)!
    var button4: Float = 2.0
   withUnsafeMutablePointer(to: &button4) { pointer in
          _ = pointer.pointee
   }
   if !disconnectx.hasPrefix("\(button4)") {
      disconnectx.append("\((Int(button4 > 129575770.0 || button4 < -129575770.0 ? 18.0 : button4) / 2))")
   }

   while (disconnectx.contains("\(button4)")) {
      disconnectx = "\(2)"
      break
   }
    var pause: UIResponder? = view
    while !(pause is UIViewController) {
        pause = pause?.next
        if pause == nil {
            return nil
        }
    }
    return pause as? UIViewController
}

func resizeImage(_ image: UIImage, maxPixelSize: CGFloat) -> UIImage? {
       var codinggM: Double = 0.0
    var codinggQ: String! = String(cString: [109,101,97,115,117,114,101,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &codinggQ) { pointer in
          _ = pointer.pointee
   }
    var placeU: String! = String(cString: [102,95,51,95,99,111,115,112,105,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &placeU) { pointer in
    
   }
      placeU.append("\((Int(codinggM > 99118902.0 || codinggM < -99118902.0 ? 31.0 : codinggM)))")

   while (placeU.count <= codinggQ.count) {
       var nameE: String! = String(cString: [100,111,112,115,0], encoding: .utf8)!
       var loveZ: String! = String(cString: [99,99,111,117,110,116,0], encoding: .utf8)!
       var launch4: [Any]! = [String(cString: [115,116,111,112,112,105,110,103,0], encoding: .utf8)!, String(cString: [117,110,112,97,100,100,101,100,0], encoding: .utf8)!]
      for _ in 0 ..< 3 {
         launch4.append(2)
      }
          var gressy: String! = String(cString: [112,104,121,115,0], encoding: .utf8)!
         loveZ.append("\(2)")
         gressy = "\(nameE.count / (Swift.max(7, loveZ.count)))"
      while ((1 + launch4.count) == 5) {
         loveZ.append("\(nameE.count)")
         break
      }
         nameE = "\(3)"
      for _ in 0 ..< 1 {
         nameE = "\(loveZ.count | launch4.count)"
      }
      repeat {
         loveZ.append("\(2 * nameE.count)")
         if (String(cString:[98,110,108,49,52,51,121,103,0], encoding: .utf8)!) == loveZ {
            break
         }
      } while ((String(cString:[98,110,108,49,52,51,121,103,0], encoding: .utf8)!) == loveZ) && (loveZ.count >= launch4.count)
      if 1 == (loveZ.count % 5) {
         launch4 = [2]
      }
       var lines3: Double = 5.0
       var rawingW: Double = 1.0
      if launch4.count <= (Int(rawingW > 234791727.0 || rawingW < -234791727.0 ? 11.0 : rawingW)) {
         launch4.append(launch4.count & loveZ.count)
      }
         lines3 /= Swift.max(2, (Double(Int(rawingW > 387060480.0 || rawingW < -387060480.0 ? 69.0 : rawingW))))
      codinggQ = "\(loveZ.count | 3)"
      break
   }
    let needs = image.size
   while (codinggQ.hasSuffix("\(codinggM)")) {
       var codebuttonY: Float = 5.0
       var scrollL: Double = 1.0
       var prime3: [Any]! = [427, 654]
      for _ in 0 ..< 2 {
         prime3 = [prime3.count]
      }
       var sandboxz: [String: Any]! = [String(cString: [117,110,102,111,99,117,115,101,100,0], encoding: .utf8)!:439, String(cString: [109,101,115,115,97,103,105,110,103,0], encoding: .utf8)!:861, String(cString: [116,105,109,101,111,117,116,101,100,0], encoding: .utf8)!:952]
       var storeC: [String: Any]! = [String(cString: [98,117,116,111,110,0], encoding: .utf8)!:662, String(cString: [118,111,114,100,105,112,108,111,109,0], encoding: .utf8)!:452]
      if storeC.keys.contains("\(codebuttonY)") {
          var edgeM: String! = String(cString: [114,97,100,98,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &edgeM) { pointer in
    
         }
          var g_center0: String! = String(cString: [110,111,114,111,117,110,100,0], encoding: .utf8)!
          _ = g_center0
         storeC = ["\(storeC.count)": storeC.count & 1]
         edgeM = "\(3 - edgeM.count)"
         g_center0 = "\(((String(cString:[112,0], encoding: .utf8)!) == g_center0 ? g_center0.count : Int(codebuttonY > 41695235.0 || codebuttonY < -41695235.0 ? 75.0 : codebuttonY)))"
      }
      if 1.84 >= scrollL {
         codebuttonY /= Swift.max(2, Float(2 >> (Swift.min(3, prime3.count))))
      }
      for _ in 0 ..< 1 {
         codebuttonY -= Float(1 / (Swift.max(9, prime3.count)))
      }
      repeat {
         prime3.append(prime3.count | storeC.keys.count)
         if prime3.count == 3483116 {
            break
         }
      } while ((4.20 * codebuttonY) >= 3.27 && (codebuttonY * 4.20) >= 5.23) && (prime3.count == 3483116)
      for _ in 0 ..< 2 {
         storeC["\(scrollL)"] = (prime3.count >> (Swift.min(2, labs(Int(scrollL > 147815042.0 || scrollL < -147815042.0 ? 5.0 : scrollL)))))
      }
      for _ in 0 ..< 3 {
         sandboxz["\(codebuttonY)"] = (Int(codebuttonY > 104417265.0 || codebuttonY < -104417265.0 ? 30.0 : codebuttonY))
      }
         prime3.append(prime3.count)
      codinggQ = "\(placeU.count ^ 1)"
      break
   }
    let pro = CGSize(width: maxPixelSize, height: maxPixelSize)
      codinggM += Double(placeU.count)
    
    
    let parameter = min(pro.width / needs.width, pro.height / needs.height)
   repeat {
       var voice8: Int = 0
       var chatdraw3: String! = String(cString: [112,95,49,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &chatdraw3) { pointer in
             _ = pointer.pointee
      }
       var collectionE: String! = String(cString: [103,109,111,99,107,0], encoding: .utf8)!
       var aymentc: Double = 4.0
       var recognitionr: String! = String(cString: [118,99,97,114,100,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         aymentc *= Double(recognitionr.count)
      }
      repeat {
          var loadr: String! = String(cString: [102,114,97,109,101,108,101,115,115,0], encoding: .utf8)!
          var font8: String! = String(cString: [109,97,105,110,115,116,97,103,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &font8) { pointer in
                _ = pointer.pointee
         }
          var register_f2: Int = 5
          var selectedb: Float = 1.0
         withUnsafeMutablePointer(to: &selectedb) { pointer in
    
         }
         collectionE.append("\(voice8 ^ 2)")
         loadr.append("\((3 >> (Swift.min(3, labs(Int(aymentc > 318081371.0 || aymentc < -318081371.0 ? 53.0 : aymentc))))))")
         font8 = "\(register_f2 >> (Swift.min(labs(1), 1)))"
         register_f2 &= ((String(cString:[80,0], encoding: .utf8)!) == collectionE ? collectionE.count : Int(aymentc > 291852737.0 || aymentc < -291852737.0 ? 38.0 : aymentc))
         selectedb /= Swift.max(1, Float(1))
         if 1263054 == collectionE.count {
            break
         }
      } while (1263054 == collectionE.count) && (4 == collectionE.count)
         voice8 &= (2 | Int(aymentc > 36945798.0 || aymentc < -36945798.0 ? 46.0 : aymentc))
         recognitionr.append("\(voice8)")
          var headl: Double = 4.0
         collectionE.append("\(2)")
         headl -= Double(collectionE.count)
         aymentc /= Swift.max(4, Double(3))
      if collectionE == chatdraw3 {
          var settingf: Double = 3.0
         withUnsafeMutablePointer(to: &settingf) { pointer in
                _ = pointer.pointee
         }
          var rmblabelj: Double = 1.0
         withUnsafeMutablePointer(to: &rmblabelj) { pointer in
                _ = pointer.pointee
         }
          var recordingvk: Double = 3.0
         chatdraw3.append("\(2 + chatdraw3.count)")
         settingf *= (Double(Int(aymentc > 165035706.0 || aymentc < -165035706.0 ? 70.0 : aymentc)))
         rmblabelj /= Swift.max(2, Double(recognitionr.count / (Swift.max(1, 8))))
         recordingvk -= Double(recognitionr.count)
      }
      while (!collectionE.hasPrefix("\(aymentc)")) {
         aymentc += (Double(chatdraw3 == (String(cString:[82,0], encoding: .utf8)!) ? recognitionr.count : chatdraw3.count))
         break
      }
      repeat {
         chatdraw3 = "\(voice8 | 1)"
         if chatdraw3 == (String(cString:[50,54,57,97,105,109,0], encoding: .utf8)!) {
            break
         }
      } while (3 > chatdraw3.count) && (chatdraw3 == (String(cString:[50,54,57,97,105,109,0], encoding: .utf8)!))
         voice8 >>= Swift.min(labs(collectionE.count / (Swift.max(10, voice8))), 2)
      for _ in 0 ..< 3 {
         aymentc -= Double(voice8 & 2)
      }
      while (voice8 == Int(aymentc)) {
          var processingr: Double = 5.0
          var historyE: Bool = false
          _ = historyE
          var home7: Int = 5
         voice8 &= recognitionr.count
         processingr += Double(1)
         historyE = 25.86 > aymentc
         home7 %= Swift.max(((String(cString:[90,0], encoding: .utf8)!) == recognitionr ? recognitionr.count : voice8), 2)
         break
      }
          var scale0: [Any]! = [UILabel()]
         withUnsafeMutablePointer(to: &scale0) { pointer in
                _ = pointer.pointee
         }
          var audioN: String! = String(cString: [114,101,109,97,112,0], encoding: .utf8)!
          var nicknamelabelZ: Int = 0
         collectionE = "\(((String(cString:[105,0], encoding: .utf8)!) == audioN ? scale0.count : audioN.count))"
         nicknamelabelZ <<= Swift.min(4, recognitionr.count)
      for _ in 0 ..< 1 {
         voice8 /= Swift.max(4, voice8 - 3)
      }
          var collectq: Float = 0.0
          _ = collectq
         voice8 <<= Swift.min(5, labs((Int(collectq > 370391902.0 || collectq < -370391902.0 ? 52.0 : collectq) * 3)))
      codinggQ = "\(2 << (Swift.min(3, collectionE.count)))"
      if 1554953 == codinggQ.count {
         break
      }
   } while (codinggM < 1.82) && (1554953 == codinggQ.count)
    let observations = CGSize(width: needs.width * parameter, height: needs.height * parameter)
    
    
    UIGraphicsBeginImageContextWithOptions(observations, true, 1.0)
    image.draw(in: CGRect(origin: .zero, size: observations))
    let originalImage = UIGraphicsGetImageFromCurrentImageContext()
    UIGraphicsEndImageContext()
    
    return originalImage
}
