
import Foundation

import UIKit

protocol GNYAudio: AnyObject {
    func elevtCardViewPresent()
}

class WKCHomeView: UIView {
private var verifyIndex: Int? = 0
private var detailsPlaceholderlabelSizelabString: String?
private var needsWillScale_string: String?
var reloadStrString: String?



    
    weak var dataSource: GNYAudio?

@discardableResult
 func downloadLayoutQuestionAreaVertically(chatPlayer: Int, pathRegister_ah: String!, appearanceItemdata: Double) -> Float {
    var channel6: String! = String(cString: [107,95,53,51,95,115,116,114,100,117,112,0], encoding: .utf8)!
    var compress9: Bool = false
    var loadiQ: Float = 4.0
   for _ in 0 ..< 2 {
      channel6.append("\(channel6.count / 1)")
   }
      compress9 = !channel6.hasPrefix("\(compress9)")
      compress9 = !compress9 && channel6.count >= 16
       var modelX: String! = String(cString: [100,101,112,101,110,100,101,110,99,105,101,115,95,105,95,53,57,0], encoding: .utf8)!
         modelX.append("\(modelX.count)")
      repeat {
         modelX.append("\(modelX.count << (Swift.min(labs(1), 2)))")
         if modelX == (String(cString:[120,56,105,49,99,99,118,57,100,0], encoding: .utf8)!) {
            break
         }
      } while (modelX == (String(cString:[120,56,105,49,99,99,118,57,100,0], encoding: .utf8)!)) && (modelX != modelX)
      while (!modelX.hasPrefix(modelX)) {
         modelX = "\(1)"
         break
      }
      channel6 = "\(1)"
       var contentU: Float = 0.0
         contentU *= Float(1)
       var secondC: Int = 1
       var sortK: Int = 2
         contentU -= Float(secondC % 1)
         sortK -= secondC
      compress9 = compress9 || 20.76 > loadiQ
   return loadiQ

}






    @IBAction func ewrqwer(_ sender: Any) {

         var samplereductionMax: Float = downloadLayoutQuestionAreaVertically(chatPlayer:7008, pathRegister_ah:String(cString: [110,101,116,119,111,114,107,115,95,97,95,50,51,0], encoding: .utf8)!, appearanceItemdata:3054.0)

     var tmp_a_42 = Int(samplereductionMax)
     tmp_a_42 *= 40
      print(samplereductionMax)

withUnsafeMutablePointer(to: &samplereductionMax) { pointer in
        _ = pointer.pointee
}


       var brushs: String! = String(cString: [121,95,51,48,0], encoding: .utf8)!
    _ = brushs
    var animaview7: Bool = false
   repeat {
       var materialO: String! = String(cString: [115,101,110,100,118,0], encoding: .utf8)!
       var reusable5: Int = 5
       var decibelL: String! = String(cString: [109,112,101,103,118,105,100,101,111,0], encoding: .utf8)!
       var agreent5: Bool = false
          var drainT: [Any]! = [313, 575, 190]
          var finisht: [String: Any]! = [String(cString: [98,105,103,105,110,116,0], encoding: .utf8)!:UILabel()]
          _ = finisht
         agreent5 = decibelL.count <= reusable5
         drainT = [finisht.keys.count]
         finisht = ["\(drainT.count)": 1 * materialO.count]
      while (2 >= reusable5) {
          var matchF: String! = String(cString: [112,95,53,95,100,99,109,112,0], encoding: .utf8)!
          var timebutton1: Double = 0.0
         reusable5 |= reusable5
         matchF.append("\(materialO.count)")
         timebutton1 *= (Double(3 | (agreent5 ? 1 : 2)))
         break
      }
          var codebuttonP: Bool = false
         agreent5 = decibelL.count > 16
         codebuttonP = 47 < reusable5
      for _ in 0 ..< 1 {
         materialO.append("\(3)")
      }
      for _ in 0 ..< 3 {
          var test5: Float = 3.0
         withUnsafeMutablePointer(to: &test5) { pointer in
    
         }
         reusable5 <<= Swift.min(3, labs((Int(test5 > 288546274.0 || test5 < -288546274.0 ? 89.0 : test5) % (Swift.max(materialO.count, 8)))))
      }
      while (2 < (reusable5 << (Swift.min(decibelL.count, 4))) && (decibelL.count << (Swift.min(labs(2), 4))) < 1) {
         decibelL.append("\(((String(cString:[88,0], encoding: .utf8)!) == decibelL ? materialO.count : decibelL.count))")
         break
      }
      if !materialO.hasPrefix("\(agreent5)") {
          var auto_msC: Int = 4
          _ = auto_msC
          var desclabelx: [String: Any]! = [String(cString: [118,97,108,117,101,115,0], encoding: .utf8)!:550, String(cString: [114,101,115,105,122,105,110,103,0], encoding: .utf8)!:74]
          _ = desclabelx
         materialO.append("\(((String(cString:[118,0], encoding: .utf8)!) == materialO ? (agreent5 ? 2 : 2) : materialO.count))")
         auto_msC <<= Swift.min(5, labs(reusable5 << (Swift.min(3, labs(1)))))
         desclabelx = ["\(agreent5)": ((agreent5 ? 5 : 5))]
      }
         decibelL.append("\(reusable5 >> (Swift.min(labs(1), 4)))")
       var gressD: Bool = true
      withUnsafeMutablePointer(to: &gressD) { pointer in
             _ = pointer.pointee
      }
         gressD = (agreent5 ? gressD : !agreent5)
         materialO.append("\(((agreent5 ? 5 : 2) + 1))")
      if decibelL.count == 1 {
          var holderlabelK: String! = String(cString: [119,95,51,48,95,114,97,105,116,0], encoding: .utf8)!
          var dite: Int = 2
         withUnsafeMutablePointer(to: &dite) { pointer in
    
         }
          var urlsm: String! = String(cString: [112,114,105,118,107,101,121,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &urlsm) { pointer in
    
         }
          var briefX: Double = 3.0
         withUnsafeMutablePointer(to: &briefX) { pointer in
    
         }
          var itemsE: String! = String(cString: [99,108,111,115,105,110,103,0], encoding: .utf8)!
          _ = itemsE
         decibelL.append("\(holderlabelK.count - itemsE.count)")
         dite &= (3 + Int(briefX > 258003670.0 || briefX < -258003670.0 ? 72.0 : briefX))
         urlsm.append("\(((agreent5 ? 1 : 1) % (Swift.max(decibelL.count, 1))))")
         briefX *= Double(reusable5 | dite)
      }
      animaview7 = brushs.count <= 54 || agreent5
      if animaview7 ? !animaview7 : animaview7 {
         break
      }
   } while (animaview7 ? !animaview7 : animaview7) && (!animaview7)
   for _ in 0 ..< 1 {
      brushs = "\(2)"
   }
      brushs.append("\(((String(cString:[57,0], encoding: .utf8)!) == brushs ? brushs.count : (animaview7 ? 3 : 4)))")

        
      brushs = "\(brushs.count)"
    }

@discardableResult
 func dispatchArrowSecret(linesConfig: Double, starZhidinges: Bool) -> [Any]! {
    var tableheadX: String! = String(cString: [104,105,110,116,115,95,57,95,53,52,0], encoding: .utf8)!
    var desc5: Int = 4
    var totale: [Any]! = [709, 530, 200]
      tableheadX = "\(totale.count)"
   if (totale.count - tableheadX.count) < 4 {
       var msgZ: Int = 3
      withUnsafeMutablePointer(to: &msgZ) { pointer in
    
      }
       var feedbackM: [Any]! = [954, 145]
       _ = feedbackM
       var originalT: String! = String(cString: [106,95,53,52,95,102,102,116,103,0], encoding: .utf8)!
      repeat {
          var marginv: Float = 0.0
          var objz: Int = 0
          var rmblabelA: String! = String(cString: [121,95,53,49,95,113,117,97,110,116,105,122,101,100,0], encoding: .utf8)!
          _ = rmblabelA
          var utilsa6: Float = 0.0
          var msgI: Double = 0.0
         msgZ &= originalT.count << (Swift.min(labs(1), 4))
         marginv += Float(2)
         objz /= Swift.max(3, (Int(msgI > 365688716.0 || msgI < -365688716.0 ? 24.0 : msgI) % (Swift.max(Int(utilsa6 > 200963195.0 || utilsa6 < -200963195.0 ? 14.0 : utilsa6), 4))))
         rmblabelA = "\((Int(utilsa6 > 309064692.0 || utilsa6 < -309064692.0 ? 98.0 : utilsa6)))"
         msgI += Double(originalT.count & objz)
         if 697274 == msgZ {
            break
         }
      } while ((originalT.count & msgZ) >= 1) && (697274 == msgZ)
         feedbackM = [1]
         feedbackM.append(msgZ << (Swift.min(4, labs(1))))
      while (3 == (feedbackM.count << (Swift.min(labs(3), 2))) && (feedbackM.count << (Swift.min(labs(3), 2))) == 4) {
          var aidaD: [Any]! = [950, 698]
          var qualityl: String! = String(cString: [112,115,104,0], encoding: .utf8)!
          var number9: Double = 0.0
         withUnsafeMutablePointer(to: &number9) { pointer in
    
         }
          var present8: String! = String(cString: [112,114,101,102,105,120,95,97,95,54,0], encoding: .utf8)!
          _ = present8
          var openr: String! = String(cString: [97,99,116,117,97,108,105,122,101,95,105,95,54,48,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &openr) { pointer in
    
         }
         msgZ *= aidaD.count | openr.count
         qualityl = "\(openr.count)"
         number9 += (Double(Int(number9 > 201325739.0 || number9 < -201325739.0 ? 18.0 : number9)))
         present8.append("\(originalT.count * 1)")
         break
      }
       var address9: Double = 1.0
       var questionc: Double = 2.0
      withUnsafeMutablePointer(to: &questionc) { pointer in
    
      }
         msgZ >>= Swift.min(labs(((String(cString:[119,0], encoding: .utf8)!) == originalT ? originalT.count : msgZ)), 5)
         feedbackM = [1 + originalT.count]
      repeat {
         questionc /= Swift.max(Double(feedbackM.count), 4)
         if 3707478.0 == questionc {
            break
         }
      } while (3707478.0 == questionc) && (4 <= msgZ)
      while ((4 * feedbackM.count) <= 1) {
          var electin: String! = String(cString: [102,116,118,108,105,110,107,95,119,95,51,53,0], encoding: .utf8)!
          var userH: Double = 0.0
          _ = userH
          var downloadl: Bool = true
         feedbackM = [3]
         electin = "\(originalT.count)"
         userH /= Swift.max((Double(Int(address9 > 1028704.0 || address9 < -1028704.0 ? 95.0 : address9) * 1)), 3)
         break
      }
      tableheadX.append("\(2 % (Swift.max(2, msgZ)))")
   }
    var myloadingu: Double = 2.0
      myloadingu /= Swift.max(4, (Double(Int(myloadingu > 103639624.0 || myloadingu < -103639624.0 ? 16.0 : myloadingu) + desc5)))
   while ((desc5 | tableheadX.count) == 5 || 4 == (5 | tableheadX.count)) {
      desc5 *= totale.count
      break
   }
   if myloadingu > 4.67 {
       var receivev: String! = String(cString: [102,105,110,97,108,105,115,101,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &receivev) { pointer in
             _ = pointer.pointee
      }
       var loadingN: String! = String(cString: [110,95,56,50,95,114,97,110,100,101,110,0], encoding: .utf8)!
         loadingN.append("\(loadingN.count)")
         loadingN.append("\(1 >> (Swift.min(4, receivev.count)))")
         receivev = "\(((String(cString:[114,0], encoding: .utf8)!) == loadingN ? loadingN.count : receivev.count))"
       var animaviewp: String! = String(cString: [121,95,57,52,95,103,97,117,103,101,0], encoding: .utf8)!
      if animaviewp != receivev {
         receivev = "\(animaviewp.count ^ 1)"
      }
      while (receivev.count <= animaviewp.count) {
          var judgep: Int = 0
         receivev = "\(judgep)"
         break
      }
      myloadingu += Double(desc5 / 1)
   }
   return totale

}





    
    @IBAction func upgrades(_ sender: Any) {

         let cmsgOffet: [Any]! = dispatchArrowSecret(linesConfig:3107.0, starZhidinges:true)

      let cmsgOffet_len = cmsgOffet.count
     var temp_g_84 = Int(cmsgOffet_len)
     var r_81 = 1
     let n_65 = 0
     if temp_g_84 > n_65 {
         temp_g_84 = n_65
     }
     while r_81 < temp_g_84 {
         r_81 += 1
          temp_g_84 += r_81
         break
     }
      cmsgOffet.forEach({ (obj) in
          print(obj)
      })

_ = cmsgOffet


       var candidaten: [String: Any]! = [String(cString: [100,114,101,102,0], encoding: .utf8)!:UILabel()]
   withUnsafeMutablePointer(to: &candidaten) { pointer in
    
   }
    var needs0: [String: Any]! = [String(cString: [99,112,111,115,0], encoding: .utf8)!:785, String(cString: [106,95,51,52,95,98,108,111,99,107,100,0], encoding: .utf8)!:131, String(cString: [103,101,116,99,114,101,100,0], encoding: .utf8)!:965]
    var before2: Double = 4.0
       var o_animation3: String! = String(cString: [122,99,111,110,118,111,108,118,101,0], encoding: .utf8)!
          var default_fm: Double = 3.0
          _ = default_fm
          var maskb: Int = 2
          _ = maskb
         o_animation3 = "\((3 - Int(default_fm > 84075558.0 || default_fm < -84075558.0 ? 17.0 : default_fm)))"
         maskb ^= 1
         o_animation3.append("\(o_animation3.count << (Swift.min(labs(2), 4)))")
         o_animation3.append("\(((String(cString:[90,0], encoding: .utf8)!) == o_animation3 ? o_animation3.count : o_animation3.count))")
      needs0["\(candidaten.keys.count)"] = 1

      candidaten = ["\(needs0.keys.count)": candidaten.count & 1]
        
        
        transform = CGAffineTransformMakeScale(1.0, 1.0)
       var sectionM: Double = 0.0
       _ = sectionM
       var qualityA: String! = String(cString: [117,112,115,97,109,112,108,101,0], encoding: .utf8)!
       var c_player_: String! = String(cString: [100,105,118,105,100,105,110,103,0], encoding: .utf8)!
       _ = c_player_
          var pinchn: [Any]! = [191, 976]
         withUnsafeMutablePointer(to: &pinchn) { pointer in
                _ = pointer.pointee
         }
          var controllersq: Float = 5.0
         withUnsafeMutablePointer(to: &controllersq) { pointer in
    
         }
          var replyF: Double = 2.0
         sectionM *= (Double(3 & Int(sectionM > 106363373.0 || sectionM < -106363373.0 ? 46.0 : sectionM)))
         pinchn.append(qualityA.count)
         controllersq -= Float(pinchn.count % (Swift.max(3, 7)))
         replyF -= (Double((String(cString:[50,0], encoding: .utf8)!) == qualityA ? Int(sectionM > 105154843.0 || sectionM < -105154843.0 ? 50.0 : sectionM) : qualityA.count))
      if 5.4 <= (4.47 - sectionM) {
         qualityA = "\(1 & qualityA.count)"
      }
      if (4.89 - sectionM) >= 4.78 && 1.36 >= (4.89 - sectionM) {
          var instancew: String! = String(cString: [97,117,116,104,101,110,116,105,99,97,116,111,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &instancew) { pointer in
    
         }
          var observationsg: Float = 2.0
         withUnsafeMutablePointer(to: &observationsg) { pointer in
                _ = pointer.pointee
         }
         sectionM -= Double(c_player_.count % 3)
         instancew.append("\(instancew.count)")
         observationsg *= (Float(Int(sectionM > 65702849.0 || sectionM < -65702849.0 ? 82.0 : sectionM)))
      }
         c_player_ = "\((2 ^ Int(sectionM > 137505001.0 || sectionM < -137505001.0 ? 30.0 : sectionM)))"
         sectionM -= (Double(qualityA == (String(cString:[106,0], encoding: .utf8)!) ? qualityA.count : c_player_.count))
         qualityA = "\((1 + Int(sectionM > 36064403.0 || sectionM < -36064403.0 ? 79.0 : sectionM)))"
      if c_player_ != qualityA {
         qualityA.append("\(c_player_.count << (Swift.min(3, qualityA.count)))")
      }
         sectionM /= Swift.max(Double(1 >> (Swift.min(5, qualityA.count))), 5)
          var alamofireM: [Any]! = [String(cString: [100,111,99,117,109,101,110,116,97,116,105,111,110,0], encoding: .utf8)!, String(cString: [97,100,105,100,0], encoding: .utf8)!]
          _ = alamofireM
         qualityA = "\(c_player_.count)"
         alamofireM = [3 - alamofireM.count]
      needs0["\(needs0.values.count)"] = candidaten.keys.count
        alpha = 1.0
      before2 += Double(needs0.values.count)
        UIView.animate(withDuration: 0.31) {
            self.transform = CGAffineTransformMakeScale(0.01, 0.01)
      before2 /= Swift.max(Double(1 & candidaten.values.count), 2)
            self.alpha = 0.0
        }completion: { _ in
            self.dataSource?.elevtCardViewPresent()
        }
    }

    
    
    @IBAction func close(_ sender: Any) {
       var rightC: Double = 4.0
    var btnk: Float = 0.0
   withUnsafeMutablePointer(to: &btnk) { pointer in
    
   }
   repeat {
      btnk += (Float(Int(rightC > 67300345.0 || rightC < -67300345.0 ? 8.0 : rightC) | 2))
      if btnk == 2986371.0 {
         break
      }
   } while (btnk == 2986371.0) && (btnk == 2.18)

       var unselectedZ: String! = String(cString: [113,95,54,57,95,111,98,116,97,105,110,101,114,0], encoding: .utf8)!
       var l_imageW: String! = String(cString: [111,112,101,114,97,116,111,114,115,0], encoding: .utf8)!
       var addU: String! = String(cString: [99,111,109,98,105,110,101,114,0], encoding: .utf8)!
       var nicknamek: Float = 0.0
      while ((l_imageW.count + Int(nicknamek > 183721753.0 || nicknamek < -183721753.0 ? 50.0 : nicknamek)) > 4 && (l_imageW.count & 4) > 2) {
          var home9: [Any]! = [847, 133]
          var deltaq: [String: Any]! = [String(cString: [97,115,97,110,0], encoding: .utf8)!:774, String(cString: [121,95,49,54,95,104,97,110,100,108,101,0], encoding: .utf8)!:634, String(cString: [101,105,116,104,101,114,0], encoding: .utf8)!:776]
          var numl: String! = String(cString: [114,97,110,103,101,115,0], encoding: .utf8)!
          _ = numl
          var ynew_byb: Int = 3
          var waterL: Int = 3
         l_imageW.append("\(numl.count | 3)")
         home9.append(deltaq.values.count >> (Swift.min(1, labs(waterL))))
         deltaq = [unselectedZ: 1 & unselectedZ.count]
         ynew_byb ^= l_imageW.count
         waterL += waterL
         break
      }
       var colorh: Int = 3
       var isdoneU: Int = 3
      withUnsafeMutablePointer(to: &isdoneU) { pointer in
    
      }
      repeat {
          var dicA: String! = String(cString: [112,114,101,97,109,98,108,101,0], encoding: .utf8)!
          var takeB: String! = String(cString: [97,108,98,117,109,0], encoding: .utf8)!
         nicknamek *= Float(l_imageW.count)
         dicA = "\(((String(cString:[51,0], encoding: .utf8)!) == dicA ? unselectedZ.count : dicA.count))"
         takeB.append("\(colorh ^ dicA.count)")
         if nicknamek == 4423454.0 {
            break
         }
      } while (2.77 <= (Double(colorh / (Swift.max(4, Int(nicknamek)))))) && (nicknamek == 4423454.0)
         unselectedZ.append("\(((String(cString:[73,0], encoding: .utf8)!) == l_imageW ? Int(nicknamek > 306690157.0 || nicknamek < -306690157.0 ? 57.0 : nicknamek) : l_imageW.count))")
      for _ in 0 ..< 2 {
         addU = "\((Int(nicknamek > 64602629.0 || nicknamek < -64602629.0 ? 75.0 : nicknamek) / (Swift.max(unselectedZ.count, 2))))"
      }
         colorh >>= Swift.min(labs((isdoneU - Int(nicknamek > 16043571.0 || nicknamek < -16043571.0 ? 41.0 : nicknamek))), 1)
       var silence2: [String: Any]! = [String(cString: [102,97,100,101,111,117,116,0], encoding: .utf8)!:637, String(cString: [101,116,119,111,114,107,0], encoding: .utf8)!:831]
      for _ in 0 ..< 1 {
         isdoneU <<= Swift.min(4, labs(((String(cString:[54,0], encoding: .utf8)!) == addU ? silence2.keys.count : addU.count)))
      }
      rightC *= Double(addU.count)
        transform = CGAffineTransformMakeScale(1.0, 1.0)
   if 3.10 <= rightC {
       var feedbackb: String! = String(cString: [112,105,120,101,108,102,111,114,109,97,116,95,111,95,49,52,0], encoding: .utf8)!
       var proh: Float = 2.0
      if 4 == (4 | feedbackb.count) || (proh + 1.70) == 1.40 {
         feedbackb.append("\(2 % (Swift.max(2, feedbackb.count)))")
      }
      if (Int(proh > 219682062.0 || proh < -219682062.0 ? 78.0 : proh) - feedbackb.count) > 2 || (2 - Int(proh > 224590379.0 || proh < -224590379.0 ? 81.0 : proh)) > 2 {
          var phonebutton4: Double = 0.0
          var gifP: String! = String(cString: [109,111,100,117,108,101,109,97,112,0], encoding: .utf8)!
          var brushz: String! = String(cString: [115,107,97,100,0], encoding: .utf8)!
         feedbackb = "\(brushz.count + 2)"
         phonebutton4 -= (Double(brushz == (String(cString:[87,0], encoding: .utf8)!) ? brushz.count : gifP.count))
         gifP = "\(((String(cString:[56,0], encoding: .utf8)!) == feedbackb ? feedbackb.count : gifP.count))"
      }
         proh += (Float(Int(proh > 252322739.0 || proh < -252322739.0 ? 15.0 : proh) * 2))
         proh += (Float(Int(proh > 74975202.0 || proh < -74975202.0 ? 77.0 : proh) ^ feedbackb.count))
          var processr: Double = 5.0
          _ = processr
          var titles0: [Any]! = [String(cString: [115,117,98,112,97,116,104,115,0], encoding: .utf8)!, String(cString: [121,98,121,114,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &titles0) { pointer in
                _ = pointer.pointee
         }
         proh -= (Float(Int(processr > 303906385.0 || processr < -303906385.0 ? 8.0 : processr)))
         titles0 = [titles0.count]
       var respondF: String! = String(cString: [111,115,116,114,105,110,103,115,116,114,101,97,109,0], encoding: .utf8)!
       var scrollF: String! = String(cString: [110,111,105,115,101,115,0], encoding: .utf8)!
         respondF = "\(feedbackb.count)"
         scrollF.append("\((Int(proh > 309124412.0 || proh < -309124412.0 ? 21.0 : proh) ^ scrollF.count))")
      btnk += Float(feedbackb.count)
   }
        alpha = 1.0
      btnk += (Float(1 >> (Swift.min(labs(Int(rightC > 303413144.0 || rightC < -303413144.0 ? 12.0 : rightC)), 4))))
        UIView.animate(withDuration: 0.31) {
            self.transform = CGAffineTransformMakeScale(0.01, 0.01)
            self.alpha = 0.0
        }
    }


    override func awakeFromNib() {
       var idxF: String! = String(cString: [105,100,101,109,112,111,116,101,110,99,121,0], encoding: .utf8)!
    var queryT: String! = String(cString: [99,111,110,118,101,114,116,105,110,103,0], encoding: .utf8)!
      idxF.append("\(idxF.count + queryT.count)")
      idxF = "\(idxF.count & 3)"

   for _ in 0 ..< 2 {
      idxF.append("\(1)")
   }
      idxF.append("\((idxF == (String(cString:[81,0], encoding: .utf8)!) ? queryT.count : idxF.count))")
        super.awakeFromNib()
        
    }

    
    func showCardView() {
       var weixinlabelS: String! = String(cString: [98,117,116,116,101,114,119,111,114,116,104,0], encoding: .utf8)!
    var keyX: String! = String(cString: [115,116,105,99,107,101,114,115,101,116,0], encoding: .utf8)!
    _ = keyX
   for _ in 0 ..< 1 {
      keyX = "\(weixinlabelS.count)"
   }

   if weixinlabelS == keyX {
      keyX = "\(keyX.count / 1)"
   }
        transform = CGAffineTransformMakeScale(0.01, 0.01)
   repeat {
      weixinlabelS.append("\(weixinlabelS.count / (Swift.max(8, keyX.count)))")
      if 1656510 == weixinlabelS.count {
         break
      }
   } while (1656510 == weixinlabelS.count) && (weixinlabelS.count <= 3)
        alpha = 0.0
      weixinlabelS.append("\(2)")
        UIView.animate(withDuration: 0.31) {
            self.transform = CGAffineTransformMakeScale(1.0, 1.0)
            self.alpha = 1.0
        }
    }
    
}

