
import Foundation

import UIKit

class YHSocketUstomCell: UICollectionViewCell {
private var video_min: Float? = 0.0
var playSpace: Double? = 0.0
private var recognizerPathLeftbuttonString: String?




    @IBOutlet weak var bgImage: UIImageView!

@discardableResult
 func notificationDownPushTestZeroTableView() -> UITableView! {
    var flowU: String! = String(cString: [112,111,108,105,99,101,95,105,95,53,52,0], encoding: .utf8)!
    var phonelabelI: [String: Any]! = [String(cString: [97,95,56,95,112,97,114,116,105,116,105,111,110,105,110,103,0], encoding: .utf8)!:377, String(cString: [100,101,99,111,109,112,114,101,115,115,95,51,95,49,49,0], encoding: .utf8)!:335]
   for _ in 0 ..< 3 {
       var strO: [String: Any]! = [String(cString: [109,97,114,107,117,112,95,100,95,52,0], encoding: .utf8)!:String(cString: [113,95,54,55,95,111,112,117,115,101,110,99,0], encoding: .utf8)!]
       _ = strO
       var colorj: String! = String(cString: [118,95,52,55,95,97,115,115,101,114,116,115,0], encoding: .utf8)!
       var deltaU: String! = String(cString: [97,114,99,104,105,118,105,110,103,0], encoding: .utf8)!
       _ = deltaU
       var voicet: Bool = true
       var editL: String! = String(cString: [105,95,56,55,95,101,120,116,101,114,110,0], encoding: .utf8)!
         editL = "\(editL.count)"
         editL.append("\(3)")
         deltaU.append("\(colorj.count)")
         strO["\(colorj)"] = 2
      repeat {
         voicet = 47 < editL.count
         if voicet ? !voicet : voicet {
            break
         }
      } while (voicet ? !voicet : voicet) && (!voicet && 2 >= deltaU.count)
          var btnO: String! = String(cString: [100,110,115,110,97,109,101,95,103,95,56,0], encoding: .utf8)!
          var true_tM: [Any]! = [892, 27, 56]
          var relationv: Float = 3.0
         deltaU = "\(2 >> (Swift.min(4, colorj.count)))"
         btnO.append("\(strO.count + 2)")
         true_tM.append(1)
         relationv -= (Float(Int(relationv > 239046064.0 || relationv < -239046064.0 ? 59.0 : relationv) | 2))
          var configD: Bool = true
          var rendererA: [Any]! = [982, 640, 795]
         withUnsafeMutablePointer(to: &rendererA) { pointer in
                _ = pointer.pointee
         }
         colorj.append("\((colorj == (String(cString:[66,0], encoding: .utf8)!) ? colorj.count : strO.count))")
         configD = deltaU.count < 99
         rendererA.append(deltaU.count)
      if !colorj.hasPrefix(deltaU) {
          var reflectH: String! = String(cString: [103,114,111,117,112,115,95,48,95,51,56,0], encoding: .utf8)!
          var datay: Bool = true
          var pricelabelb: [String: Any]! = [String(cString: [103,101,116,112,97,103,101,115,105,122,101,95,107,95,53,55,0], encoding: .utf8)!:731, String(cString: [112,111,108,121,115,116,97,114,95,53,95,50,52,0], encoding: .utf8)!:487, String(cString: [107,95,50,48,95,113,117,97,110,116,120,0], encoding: .utf8)!:316]
         withUnsafeMutablePointer(to: &pricelabelb) { pointer in
    
         }
          var filemI: String! = String(cString: [99,95,49,54,95,112,97,114,115,101,100,0], encoding: .utf8)!
         deltaU = "\((strO.values.count / (Swift.max(1, (voicet ? 1 : 3)))))"
         reflectH = "\(editL.count)"
         datay = filemI.count > 29 || !datay
         pricelabelb["\(voicet)"] = 2
         filemI.append("\(reflectH.count)")
      }
         strO = ["\(voicet)": (deltaU == (String(cString:[55,0], encoding: .utf8)!) ? deltaU.count : (voicet ? 3 : 3))]
         deltaU = "\(3 - strO.values.count)"
      while (2 <= (colorj.count ^ 3)) {
          var stra: Float = 5.0
         strO = ["\(voicet)": 1]
         stra /= Swift.max(4, Float(2 / (Swift.max(3, deltaU.count))))
         break
      }
          var pathsJ: String! = String(cString: [97,99,104,101,95,48,95,51,57,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &pathsJ) { pointer in
                _ = pointer.pointee
         }
          var colorA: Double = 1.0
         strO = [editL: editL.count << (Swift.min(labs(1), 3))]
         pathsJ = "\(strO.keys.count)"
         colorA += (Double(editL.count & (voicet ? 4 : 5)))
         deltaU = "\(3 - strO.count)"
      repeat {
         colorj.append("\((deltaU == (String(cString:[106,0], encoding: .utf8)!) ? (voicet ? 2 : 5) : deltaU.count))")
         if colorj.count == 538553 {
            break
         }
      } while ((colorj.count + strO.values.count) <= 5) && (colorj.count == 538553)
          var iconn: Float = 1.0
         withUnsafeMutablePointer(to: &iconn) { pointer in
    
         }
         voicet = ((Int(iconn > 371843558.0 || iconn < -371843558.0 ? 56.0 : iconn) + colorj.count) < 10)
      flowU = "\(deltaU.count)"
   }
   if 2 <= (phonelabelI.values.count + flowU.count) {
      phonelabelI = ["\(phonelabelI.keys.count)": flowU.count - 2]
   }
   for _ in 0 ..< 3 {
      phonelabelI["\(flowU)"] = 2
   }
       var prepareI: Bool = false
      while (prepareI && !prepareI) {
         prepareI = !prepareI
         break
      }
       var text5: Double = 0.0
      for _ in 0 ..< 2 {
         prepareI = 52.86 == text5
      }
      flowU.append("\((phonelabelI.values.count | (prepareI ? 2 : 1)))")
     var screenItemdata: UILabel! = UILabel()
     let expireCompress: String! = String(cString: [112,117,110,99,104,95,108,95,57,52,0], encoding: .utf8)!
     var ratioCompletion: UIImageView! = UIImageView(image:UIImage(named:String(cString: [118,95,53,55,95,100,101,112,114,101,99,105,97,116,101,100,104,0], encoding: .utf8)!))
    var newlyBold:UITableView! = UITableView(frame:CGRect.zero)
    screenItemdata.alpha = 0.4;
    screenItemdata.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    screenItemdata.frame = CGRect(x: 313, y: 82, width: 0, height: 0)
    screenItemdata.text = ""
    screenItemdata.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    screenItemdata.textAlignment = .left
    screenItemdata.font = UIFont.systemFont(ofSize:19)
    
    var screenItemdataFrame = screenItemdata.frame
    screenItemdataFrame.size = CGSize(width: 139, height: 180)
    screenItemdata.frame = screenItemdataFrame
    if screenItemdata.isHidden {
         screenItemdata.isHidden = false
    }
    if screenItemdata.alpha > 0.0 {
         screenItemdata.alpha = 0.0
    }
    if !screenItemdata.isUserInteractionEnabled {
         screenItemdata.isUserInteractionEnabled = true
    }

    ratioCompletion.alpha = 0.6;
    ratioCompletion.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    ratioCompletion.frame = CGRect(x: 300, y: 177, width: 0, height: 0)
    ratioCompletion.animationRepeatCount = 6
    ratioCompletion.image = UIImage(named:String(cString: [109,97,114,107,0], encoding: .utf8)!)
    ratioCompletion.contentMode = .scaleAspectFit
    
    var ratioCompletionFrame = ratioCompletion.frame
    ratioCompletionFrame.size = CGSize(width: 134, height: 131)
    ratioCompletion.frame = ratioCompletionFrame
    if ratioCompletion.isHidden {
         ratioCompletion.isHidden = false
    }
    if ratioCompletion.alpha > 0.0 {
         ratioCompletion.alpha = 0.0
    }
    if !ratioCompletion.isUserInteractionEnabled {
         ratioCompletion.isUserInteractionEnabled = true
    }

    newlyBold.delegate = nil
    newlyBold.dataSource = nil
    newlyBold.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    newlyBold.alpha = 0.5;
    newlyBold.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    newlyBold.frame = CGRect(x: 290, y: 303, width: 0, height: 0)

    
    var newlyBoldFrame = newlyBold.frame
    newlyBoldFrame.size = CGSize(width: 269, height: 187)
    newlyBold.frame = newlyBoldFrame
    if newlyBold.alpha > 0.0 {
         newlyBold.alpha = 0.0
    }
    if newlyBold.isHidden {
         newlyBold.isHidden = false
    }
    if !newlyBold.isUserInteractionEnabled {
         newlyBold.isUserInteractionEnabled = true
    }

    return newlyBold

}





    override func awakeFromNib() {

         let positionsHaldclut: UITableView! = notificationDownPushTestZeroTableView()

      if positionsHaldclut != nil {
          let positionsHaldclut_tag = positionsHaldclut.tag
     var tmp_f_10 = Int(positionsHaldclut_tag)
     var b_64: Int = 0
     let s_45 = 2
     if tmp_f_10 > s_45 {
         tmp_f_10 = s_45

     }
     if tmp_f_10 <= 0 {
         tmp_f_10 = 2

     }
     for q_19 in 0 ..< tmp_f_10 {
         b_64 += q_19
     var l_45 = b_64
          if l_45 < 512 {
          l_45 += 16
          }
     else {
          l_45 *= 64

     }
         break

     }
          self.addSubview(positionsHaldclut)
      }
      else {
          print("positionsHaldclut is nil")      }

_ = positionsHaldclut


       var gund2: Bool = true
   withUnsafeMutablePointer(to: &gund2) { pointer in
    
   }
    var notification7: Bool = true
       var lineW: [Any]! = [40, 323, 635]
       var reusableS: String! = String(cString: [116,97,112,116,105,99,0], encoding: .utf8)!
       var drainx: Int = 0
       _ = drainx
         drainx %= Swift.max(reusableS.count, 1)
         lineW.append(1 & reusableS.count)
       var shu6: String! = String(cString: [102,111,114,109,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &shu6) { pointer in
    
      }
       var compress4: String! = String(cString: [108,105,98,99,0], encoding: .utf8)!
      if 4 >= lineW.count {
          var resolutiona: String! = String(cString: [102,97,118,105,99,111,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &resolutiona) { pointer in
                _ = pointer.pointee
         }
          var enginem: [String: Any]! = [String(cString: [117,112,109,105,120,0], encoding: .utf8)!:626, String(cString: [112,114,101,117,112,108,111,97,100,0], encoding: .utf8)!:280, String(cString: [102,100,99,116,120,0], encoding: .utf8)!:905]
         withUnsafeMutablePointer(to: &enginem) { pointer in
    
         }
          var stringS: String! = String(cString: [98,117,98,98,108,101,0], encoding: .utf8)!
          var o_centerR: Double = 0.0
         withUnsafeMutablePointer(to: &o_centerR) { pointer in
    
         }
         compress4.append("\(drainx)")
         resolutiona = "\(reusableS.count)"
         enginem[resolutiona] = resolutiona.count
         stringS = "\((Int(o_centerR > 287163489.0 || o_centerR < -287163489.0 ? 21.0 : o_centerR)))"
         o_centerR += Double(3 >> (Swift.min(3, lineW.count)))
      }
      while (shu6.count < 3) {
         lineW = [compress4.count]
         break
      }
          var sharem: Double = 2.0
          var main_eT: [String: Any]! = [String(cString: [119,114,97,112,100,101,116,101,99,116,0], encoding: .utf8)!:807, String(cString: [114,101,112,101,97,116,101,100,108,121,0], encoding: .utf8)!:760]
         withUnsafeMutablePointer(to: &main_eT) { pointer in
    
         }
         compress4 = "\(2)"
         sharem /= Swift.max(Double(main_eT.values.count), 5)
         main_eT = [reusableS: drainx / (Swift.max(reusableS.count, 3))]
         drainx *= 2 + lineW.count
         lineW = [lineW.count >> (Swift.min(compress4.count, 5))]
         compress4 = "\(lineW.count * reusableS.count)"
      gund2 = !reusableS.hasSuffix("\(gund2)")
   if !gund2 {
      notification7 = (notification7 ? gund2 : !notification7)
   }

   repeat {
      gund2 = (!notification7 ? !gund2 : !notification7)
      if gund2 ? !gund2 : gund2 {
         break
      }
   } while (!gund2) && (gund2 ? !gund2 : gund2)
   while (!notification7) {
      notification7 = gund2
      break
   }
        super.awakeFromNib()
        
    }

}
