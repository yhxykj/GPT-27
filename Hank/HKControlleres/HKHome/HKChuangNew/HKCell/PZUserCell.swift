
import Foundation

import UIKit

class PZUserCell: UICollectionViewCell {
private var failedSpace: Double? = 0.0
var subviewMonthDictionary: [String: Any]?



    
    @IBOutlet weak var label: UILabel!
    @IBOutlet weak var cell_icon: UIImageView!
    @IBOutlet weak var cell_love_button: UIButton!

@discardableResult
 func availableProductRemoveNormalPriceCoverLabel(appRight: [Any]!, ustomSelectindex: Bool, codebuttonAilabel: [String: Any]!) -> UILabel! {
    var isdraw7: Int = 5
    var completion6: Double = 3.0
    _ = completion6
   repeat {
      isdraw7 %= Swift.max(isdraw7, 3)
      if 221754 == isdraw7 {
         break
      }
   } while (221754 == isdraw7) && ((Double(Double(isdraw7) / (Swift.max(completion6, 9)))) > 4.92)
      isdraw7 /= Swift.max(1, isdraw7 & 3)
     let refreshDit: String! = String(cString: [114,101,99,101,105,118,101,114,95,54,95,53,50,0], encoding: .utf8)!
     let confirmbuttonPress: UIImageView! = UIImageView(image:UIImage(named:String(cString: [106,95,54,56,95,97,115,115,101,115,115,109,101,110,116,0], encoding: .utf8)!))
     let loadCompressed: Double = 82.0
    var findasocInterpTweaks:UILabel! = UILabel(frame:CGRect.zero)
    confirmbuttonPress.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    confirmbuttonPress.alpha = 0.3
    confirmbuttonPress.frame = CGRect(x: 77, y: 115, width: 0, height: 0)
    confirmbuttonPress.animationRepeatCount = 7
    confirmbuttonPress.image = UIImage(named:String(cString: [105,109,103,0], encoding: .utf8)!)
    confirmbuttonPress.contentMode = .scaleAspectFit
    
    var confirmbuttonPressFrame = confirmbuttonPress.frame
    confirmbuttonPressFrame.size = CGSize(width: 237, height: 208)
    confirmbuttonPress.frame = confirmbuttonPressFrame
    if confirmbuttonPress.alpha > 0.0 {
         confirmbuttonPress.alpha = 0.0
    }
    if confirmbuttonPress.isHidden {
         confirmbuttonPress.isHidden = false
    }
    if !confirmbuttonPress.isUserInteractionEnabled {
         confirmbuttonPress.isUserInteractionEnabled = true
    }

    findasocInterpTweaks.alpha = 0.5;
    findasocInterpTweaks.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    findasocInterpTweaks.frame = CGRect(x: 69, y: 42, width: 0, height: 0)
    findasocInterpTweaks.font = UIFont.systemFont(ofSize:14)
    findasocInterpTweaks.text = ""
    findasocInterpTweaks.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    findasocInterpTweaks.textAlignment = .right

    
    var findasocInterpTweaksFrame = findasocInterpTweaks.frame
    findasocInterpTweaksFrame.size = CGSize(width: 275, height: 227)
    findasocInterpTweaks.frame = findasocInterpTweaksFrame
    if findasocInterpTweaks.isHidden {
         findasocInterpTweaks.isHidden = false
    }
    if findasocInterpTweaks.alpha > 0.0 {
         findasocInterpTweaks.alpha = 0.0
    }
    if !findasocInterpTweaks.isUserInteractionEnabled {
         findasocInterpTweaks.isUserInteractionEnabled = true
    }

    return findasocInterpTweaks

}





    
    override func awakeFromNib() {

         var leayLangs: UILabel! = availableProductRemoveNormalPriceCoverLabel(appRight:[825, 872, 598], ustomSelectindex:false, codebuttonAilabel:[String(cString: [114,95,55,53,95,117,116,104,111,114,0], encoding: .utf8)!:String(cString: [112,114,97,103,109,97,95,121,95,50,0], encoding: .utf8)!, String(cString: [99,102,104,100,95,120,95,54,54,0], encoding: .utf8)!:String(cString: [98,95,54,57,0], encoding: .utf8)!, String(cString: [98,95,52,53,95,100,112,116,114,115,0], encoding: .utf8)!:String(cString: [109,97,103,110,105,116,117,100,101,115,0], encoding: .utf8)!])

      if leayLangs != nil {
          self.addSubview(leayLangs)
          let leayLangs_tag = leayLangs.tag
     var g_97 = Int(leayLangs_tag)
     if g_97 >= 54 {
          g_97 /= 5
          }
     else if g_97 < 503 {
          g_97 /= 22
          g_97 -= 83

     }
      }
      else {
          print("leayLangs is nil")      }

withUnsafeMutablePointer(to: &leayLangs) { pointer in
    
}


       var ailabelT: Bool = true
    var proh: String! = String(cString: [114,101,118,105,101,119,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &proh) { pointer in
    
   }
    var inewsv: String! = String(cString: [115,121,110,99,109,97,114,107,101,114,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
      inewsv = "\(proh.count % (Swift.max(1, 2)))"
   }
   if 1 >= proh.count {
      proh = "\(proh.count)"
   }

      ailabelT = !ailabelT && inewsv.count <= 25
   while (!ailabelT) {
      ailabelT = !ailabelT
      break
   }
        super.awakeFromNib()
   while (!proh.hasPrefix("\(inewsv.count)")) {
       var queryZ: Float = 4.0
      withUnsafeMutablePointer(to: &queryZ) { pointer in
             _ = pointer.pointee
      }
       var main_sr: Int = 1
       var tipg: Bool = true
       var deltaS: Double = 2.0
       var firstM: [Any]! = [310, 541, 110]
      withUnsafeMutablePointer(to: &firstM) { pointer in
    
      }
      if deltaS < 4.14 {
         tipg = queryZ >= 71.5
      }
         firstM = [((tipg ? 1 : 1) >> (Swift.min(labs(Int(deltaS > 180344471.0 || deltaS < -180344471.0 ? 44.0 : deltaS)), 5)))]
         main_sr /= Swift.max(2 & firstM.count, 3)
         firstM.append((Int(deltaS > 176404855.0 || deltaS < -176404855.0 ? 62.0 : deltaS)))
         main_sr += (Int(deltaS > 106589827.0 || deltaS < -106589827.0 ? 47.0 : deltaS) ^ main_sr)
         firstM = [(2 - Int(deltaS > 41485874.0 || deltaS < -41485874.0 ? 26.0 : deltaS))]
       var flagt: String! = String(cString: [115,117,98,115,101,116,0], encoding: .utf8)!
       var water6: String! = String(cString: [115,117,103,103,101,115,116,101,100,0], encoding: .utf8)!
          var progressL: Bool = true
          var pricelabelJ: Float = 2.0
         queryZ += (Float(flagt.count & Int(pricelabelJ > 21535288.0 || pricelabelJ < -21535288.0 ? 34.0 : pricelabelJ)))
         progressL = !tipg
      repeat {
         firstM.append((water6.count * Int(deltaS > 210054472.0 || deltaS < -210054472.0 ? 72.0 : deltaS)))
         if 2908197 == firstM.count {
            break
         }
      } while (2908197 == firstM.count) && (4 <= (firstM.count / 3))
      repeat {
         firstM.append(3)
         if 795866 == firstM.count {
            break
         }
      } while (795866 == firstM.count) && ((firstM.count + 2) > 3)
      if (3 ^ firstM.count) > 1 {
         tipg = !tipg
      }
      while (Double(main_sr) < deltaS) {
         main_sr >>= Swift.min(labs(1), 5)
         break
      }
         deltaS *= Double(firstM.count + main_sr)
         main_sr >>= Swift.min(3, labs(((tipg ? 2 : 5) & Int(deltaS > 67816076.0 || deltaS < -67816076.0 ? 82.0 : deltaS))))
      if 4 >= firstM.count {
         firstM.append(water6.count)
      }
      proh = "\(inewsv.count)"
      break
   }
   repeat {
       var timebutton3: String! = String(cString: [111,98,118,105,111,117,115,108,121,0], encoding: .utf8)!
         timebutton3.append("\(timebutton3.count / (Swift.max(2, 3)))")
      while (5 < timebutton3.count && 5 < timebutton3.count) {
          var addressT: String! = String(cString: [108,111,97,100,115,0], encoding: .utf8)!
          _ = addressT
         timebutton3 = "\(addressT.count / (Swift.max(timebutton3.count, 7)))"
         break
      }
          var offset9: String! = String(cString: [118,112,120,101,110,99,0], encoding: .utf8)!
         timebutton3.append("\(((String(cString:[103,0], encoding: .utf8)!) == offset9 ? timebutton3.count : offset9.count))")
      proh = "\(timebutton3.count % (Swift.max(10, inewsv.count)))"
      if proh.count == 2861444 {
         break
      }
   } while (inewsv == proh) && (proh.count == 2861444)
        
        layer.cornerRadius = 13
        layer.masksToBounds = true
    }
}
